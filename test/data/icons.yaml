manager:
  plus: icon-plus
  ui:
    angle:
      up: 1
      right: 2
      down: 3
      left: ~str(${down + 1})
  port: icon-${root.port}
admin: ~include(https://raw.githubusercontent.com/borgius/yamlx/master/test/data/admin.yaml)
